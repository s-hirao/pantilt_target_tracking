自分のページ
https://github.com/s-hirao/

ターミナル上
roscore
roslaunch roscore_communication roscore_communication.launch
rosparam set use_sim_time true
rosrun pantilt_velocity_calculation pantilt_velocity_calculation_node
rostopic echo -p /VelocityData > ~/pantilt_Rotation_vector_csv/rotation_0max_5.csv
rostopic echo -p ~/hirao_experiment/pantilt_position_control/
rosbag play --clock (name).bag 
roslaunch zed_wrapper zed.launch

github
git status
git log
git add -A  
git commit -m "first commit"
git remote add origin https://github.com/s-hirao/pantilt_target_tracking
git push origin master
git branch --set-upstream-to=origin/master master
git pull

lattepanda上
sudo chmod 777 /dev/ttyUSB0
roslaunch beego_control beego_control.launch

ネットワーク設定
sudo nmcli connection up　hidakaLab-experiment-2.4G

csv変換
rosrun rosbag_to_csv rosbag_to_csv.py


